# Bug Description

**Error:** The recursive function lacks a base case, causing it to recurse infinitely until it hits the maximum recursion depth, resulting in a `RecursionError`.

**Hint:** Missing base case.

### Changes to be made in main.py to fix the bug:
- Add a base case to the recursive function to define the simplest instance of the problem that can be solved without further recursion.
